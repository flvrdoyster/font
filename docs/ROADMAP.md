# 도깨비DNR 고딕 — 로드맵

**목표**: 도깨비 디나루 16×16 비트맵을 Regular(1px 줄기)·Bold(2px 줄기) 한
패밀리로 재설계하고, Regular를 [gensei-pc98](../../gensei-pc98) PC-98
에뮬레이터의 실제 한글 폰트로 납품한다.

---

## 현재 상태

한글 11,172음절 + 호환 자모 93자, 라틴/숫자/핵심 기호가 두 웨이트에 완전히
동일한 cmap으로 들어 있다. 라틴/숫자/기호는 잉크 몸통 기준 비례 스페이싱 +
실측 커닝(`tools/spacing.py`, `tools/kerning.py`), 한글은 고정폭 그대로.
gensei-pc98 납품 완료, 웹폰트(WOFF2) + GitHub Pages 자동 배포 완료. 일본어
(가나·한자)와 원본 비트맵의 상속 잡동사니(그리스·키릴·원문자 등 669자)는
범위 밖으로 확정 제외했다.

| 항목 | 상태 |
|------|------|
| 한글 음절 | 11,172자 완료 |
| 호환 자모 (`ㄱㄴㄷ`·`ㅏㅑㅓ`, 옛한글 포함) | 93자 완료 |
| Regular ↔ Bold cmap | 완전 일치 |
| 부품 조합 셀 (`tools/component_library.json`) | 1,156칸 완료, 벽돌 0 |
| `scripts/verify_ttf.py` | 불일치 0건 (양쪽) |

---

## 설계

- **한 RIBBI 패밀리**: 1px 줄기 = Regular(기본), 2px 줄기 = Bold. 내부 코드의
  `weight` 키(`regular`=2px, `light`=1px)와 파일명(`glyphs_bold.json`=2px,
  `glyphs_light.json`=1px)은 여전히 **줄기 두께**를 뜻하며 패밀리 병합과
  무관하다. 컴파일된 OpenType 이름/OS2/fsSelection만 Regular/Bold
  (`tools/metadata.py`).
  - 라틴/숫자: 2px를 손으로 그리고 1px는 얇혀서 파생(`tools/thin_vertical.py`).
  - 한글: 2px는 원본 비트맵을 픽셀-정확 벡터화, 1px는 이를 얇힌 뒤 손으로 확정.
- **획 문법**: 2px는 세로 줄기 2px·가로 바 1px·대각선 1px 계단. 1px는 세로
  줄기만 1px.
- **수직 메트릭스**: 16행 셀에서 한글은 상하 1px씩 여백을 두고 13행을 쓴다.
  라틴은 베이스라인부터 캡하이트까지 11행 — 그 안에 엑스하이트·어센더·
  디센더가 들어간다.
- **범위**: 한글 음절(11,172) + 손그림(`glyphs_bold.json`/`glyphs_light.json`)
  만 싣는다(`build_ufo.py`의 `_in_scope()`). 원본 비트맵엔 이 서체 디자인이
  아닌 상속 글리프가 섞여 있어서(그리스·키릴·원문자·괄호 한글 등 669자 —
  글자마다 잉크 위치가 제각각이고 이 서체의 수직 메트릭스(13행/11행 규칙)를
  안 지킴) 명시적으로 제외한다. 가나·한자도 같은 이유로 상시 제외
  (`_KANA_RANGES`) — 반각 CJK 문장부호 `｡｢｣､`(U+FF61-64)는 가나가 아니라서
  예외.
- **에디터**(`tools/pixel_editor.html`): 상단 탭으로 Regular/Bold 분리, 저장
  파일도 웨이트별 독립. 빌드(`scripts/build_ufo.py --weight regular|light`)도
  동일하게 대응.

---

## 한글 11,172자 — 부품 조합 모델

확정 2,350자(KS X 1001, `glyphs_light.json`)는 손그림 그대로 verbatim 유지.
나머지 8,822자는 자모 부품 조합으로 채운다(`scripts/compose_components.py`).

- **벌식(context-dependent variants)이 필요하다** — 부품이 단순 가법적이지
  않음을 실측(48,063쌍 검정, 68%만 가법적). 초성+중성을 합친 `LV`셀
  798개(`(초성,중성,받침유무)`) + 종성 `T`셀 358개(`(종성,중성)`, 겹받침
  11종만 폭 기준 2벌) = 부품셀 1,156개.
- **추출은 PC-98 완성형 비트맵의 받침 변이 zone**으로 하고, KS X 1001 밖
  대표 음절(부품 셀 에디터에서 그림)은 이미 측정된 부품을 **뺄셈**으로
  분리한다. 알고리즘의 안전장치(`LV_FLOOR`, zone 누출 처리 등)는
  `compose_components.py`의 함수 docstring이 최신 기준 — 여기 중복 서술하지
  않는다.
- **추출 결과는 `tools/component_library.json`에 고정(freeze)**되어 있고,
  실제 폰트 빌드는 이 파일과 확정 코퍼스만 읽는다 — PC-98 비트맵 없이도
  빌드된다(자기 완결적). 재고정은 `compose_components.py --freeze`를 수동
  실행할 때만 일어난다(에디터 빌드 버튼은 자동으로 안 함 — 검수 통과한
  라이브러리를 조용히 갈아치우지 않기 위해).
- **품질 체(sieve)는 "확정 손그림에 절대 안 나오는 형태"만 유효**하고, 그
  기준을 통과한 게 둘이다: 3×3 꽉 찬 덩어리(벽돌, `--blobs`)와 정확히 2px
  폭 세로 런(`--stems` — Regular 획 문법은 세로 줄기 1px인데, 두 부품이
  모음 다리 위치를 1px 다르게 알면 합쳐져 2px 기둥이 됨). 둘 다 확정
  4,156자에서 0건임을 실측하고 채택했다. 반면 샘플 불일치·평균 픽셀 오차는
  1px 광학 보정을 오답으로 세는 지표라 품질 판단에 못 쓴다
  (`--validate`/`--loo` 숫자를 볼 때 항상 이 전제 위에서 읽을 것).
- **미확정 음절의 손확정 우선순위는 실사용 빈도로** 정한다
  (`scripts/freq_worklist.py`, 구어 코퍼스 대상). 실측: 확정 4,156자가 이미
  실사용의 99.983%를 커버하고, 나머지 노출은 미확정 250종(대부분 됬·쫒·잌
  같은 구어 표기)에 몰려 있어 상위 50자만 확정해도 그 노출의 91%가 사라진다.
- 참조 데이터 원본은 `original/pc98_font.bmp` — gensei-pc98이 한때 자기
  자신(도깨비DNR)으로 교체됐던 오염본이 아니라 순정본(2026-07-31 확인·복구).

---

## 완성형 2,350자 확정 방식

대표 음절(초성 19×6블록 + 홑/겹받침, 총 166자)을 PC-98 둥근모꼴과 원본
도깨비 디나루를 겹쳐 보며 손으로 그리고, 자음을 **모임꼴**(모음 배치에 따른
분류 — 세로모임·가로모임·섞임모임, 받침 유무로 다시 갈림. 코드의
`vgroup()`/`cho_bt()`)별로 나머지 글자에 적용해 2,350자를 채운 뒤
(`compose_light.py`) 전량 검수·확정했다. 이 도구는 KS X 1001 밖을 못 다뤄
위 11,172자 확장엔 안 쓰인다.

---

## 반각 한글 (gensei-pc98 전용)

폰트 빌드와 별개. PC-98 ROM의 반각 한글 표(`font.bmp` col10-11, 유니코드
반각 자모 블록과 무관한 ROM 고유 배열)의 188칸 중 122칸(원본 잉크 있는 칸
전부)을 `tools/halfwidth_editor.html`로 손그림 완료
(`tools/glyphs_halfwidth.json`). `scripts/build_pc98_bmp.py`가 완성형 한글과
함께 `font_light.bmp`에 반영한다. `build_ufo.py`(TTF/OTF)는 이 데이터를
읽지 않는다.

---

## 이력

날짜는 그날 실제로 있었던 커밋 기준(git log). 같은 날 여러 건이면 한 항목에
묶는다.

- **2026-07-21** 라틴/숫자 완성, 완성형 2,350자 두 웨이트 확정, gensei-pc98
  납품 완료. 가나 251자를 손그림했으나 간격 문제로 이후 범위에서 제외(아래).
- **2026-07-29** 조합 추출 알고리즘 버그 수정(`jong_zone`이 초성 픽셀을
  물고 있던 것 등) — 벽돌 159자 → 0자.
- **2026-07-31** 전체 11,172자 확장 빌드 통합. `stuck` 48셀 수정(표본 부족
  시 같은 모음군 풀링). PC-98 참조가 한때 자기 자신(도깨비DNR)으로 오염돼
  있던 걸 발견·복구, 부품 라이브러리를 `component_library.json`로 고정해
  빌드를 자기 완결화. 웹폰트 빌드 + GitHub Actions/Pages 배포 추가.
- **2026-08-02** ㅞ/ㅝ 24칸 해결(zone 누출 픽셀을 기각 대신 종성으로 이동).
  가나·한자를 범위에서 완전히 제외 확정 — 간격 문제로 251자 재드로잉이
  필요했던 가나, 원래 손그림한 적 없는 한자 둘 다 이 프로젝트 목표(한글
  서체 + gensei-pc98 납품)에 불필요. 관련 파일·데이터 전량 삭제(git 이력엔
  남음).
- **2026-08-03** 원본 비트맵의 상속 글리프 669자(그리스·키릴·원문자 등)를
  Bold에서 제외 — Regular와 cmap이 정확히 일치하도록. 그중 호환 자모 93자
  (`ㄱㄴㄷ`·`ㅏㅑㅓ`, 옛한글 포함 — 조판 규칙 없이 낱자로 완결되는 유일한
  자모 코드)는 새로 손그림해 양쪽 웨이트에 편입. 이어서 품질 패스: TTF에
  smart dropout `prep` 추가(1px 획의 소실 방지, fontbakery FAIL 0), 수직
  메트릭스 lineGap 0으로 통일(모든 플랫폼 줄높이 = 정확히 1em/16px —
  이전엔 hhea/typo만 128이라 플랫폼별 16px/18px로 갈라졌음), 2px 줄기 체
  추가(조합분 20자 검출), 빈도 워크리스트 도구, `/preview` 교정쇄 프리셋
  (본문·기호·자모·검수).
- **2026-08-04** 스페이싱을 bbox 기준에서 몸통(body) 기준으로 재설계
  (`spacing.py`) — 세로 획 하나짜리 돌출(flare)이 좌우 베어링을 왜곡하던
  문제를 잡음. 잉크 경계 기반 실측 커닝(`kerning.py`) 추가, `build_ufo.py`가
  UFO kern1/kern2로 반영. box-drawing 문자가 잘못된 비례 폭을 받던 기존
  버그(v1.0.1부터)도 같이 수정. 이어서 공개 페이지(`web/index.html`)와
  편집기 `/preview`를 한 파일로 통합 — 로컬/공개가 완전히 같은 마크업을
  쓰고, 프리셋 문구는 `tools/preview_presets.js` 하나로 공유. 히어로
  섹션·강조색(`--accent` 스케일)·픽셀 폰트 적용 범위 등 디자인 정리.
- **2026-08-04 ~ 08-16** 알파벳·숫자·기호를 Fixedsys Excelsior
  (`refs/FSEX302.ttf`, CC0)와 실측 대조해 M/W 폭·`@`/`*`/`"`/`&` 등 여러
  글자 재작업. 편집기에 참조 오버레이를 레이어별(웨이트/외부 참조/참고
  글자) 토글로 분리하고, Bold 탭엔 Fixedsys, Regular 탭의 전각 영숫자엔
  PC-98 원본(`tools/pc98_fullwidth.py`, JIS ku 3)을 붙임. 반각 편집기의
  "참고" 오버레이도 ROM 원본 대신 이미 그려둔 다른 슬롯을 트레이싱하는
  방식으로 교체(같은 이유 — ROM 반각 칸 자체가 이 프로젝트가 손으로 채운
  것이라 독립된 참조가 못 됨). `build_pc98_bmp.py`가 전각 영숫자까지
  `font_light.bmp`에 반영하도록 확장.

---

## 검증

`scripts/verify_ttf.py`는 웨이트별로 "의도한 값"을 다르게 비교한다 — Bold는
원본 비트맵(`glyphs_bold.json` 손교정 우선), Regular는
`glyphs_light.json` 손그림 ∪ 부품 조합 결과. 둘 다 `_in_scope()`로 걸러진
글리프는 기대값에서도 제외해 가짜 불일치를 막는다. `--proportional` 빌드의
좌우 이동(`tools/spacing.py`)도 반영. 현재 둘 다 불일치 0건 — 시각 QA +
fontbakery는 별도로 병행한다.
